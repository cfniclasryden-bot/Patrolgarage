{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python3 scripts/cron_entry.py",
    "restartPolicyType": "NEVER",
    "cronSchedule": "*/5 * * * *"
  }
}
