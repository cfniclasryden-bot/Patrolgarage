research/
drafts/
.DS_Store
__pycache__/
*.pyc
.env

# Local Netlify folder
.netlify
logs/

# Internal docs — not for the public site (also blocked via /docs/* 404 redirect)
docs/

# GSC OAuth refresh token (credential) — never commit
.gsc-oauth.json
